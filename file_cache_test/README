Author: Arijit Sarcar

Pre-requisite Packages: Please ensure that the following packages are already
installed (# packages are optional):
- cmake
# - ccache
# - distcc & export DISTCC_DISABLE=1 
# - libboost-all-dev
- libgoogle-glog-dev (for google logging - google flags installed as depended package)
- libgoogle-perftools-dev (for thread malloc and profiling heap & cpu)
# - emacs23 (editor) & load google-c-style.el
# libprotobuf-dev

Build the file cache library and the test cases
> mkdir -p $TOP_DIR/build
> cd $TOP_DIR/build
> cmake VERBOSE=1 ../src -DCMAKE_BUILD_TYPE=Debug
> make
> make test

